(ns oc.storage.db.migrations.create-videos
  (:require [rethinkdb.query :as r]
            [oc.lib.db.migrations :as m]
            [oc.storage.config :as c]
            [oc.storage.resource.video :as video))

(defn up [conn]
  (println "Creating table: " video/table-name)
  (println (m/create-table conn c/db-name video/table-name video/primary-key))
  true) ; return true on success